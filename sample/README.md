# Test Samples

This directory contains a small, neutral sample for testing DevCleaner.

Objective:
- Include both used and unused imports,
- Include TS and TSX examples,
- Maintain a simple structure.

Structure:
- `basic/`: Simple TypeScript examples.
- `web/`: TSX examples for validating parsing with JSX.